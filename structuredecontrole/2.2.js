let min = prompt("Please enter a minimum number");
let max = prompt("Pease enter a maximum number");
let current = prompt ("Pease enter an other number");


if ((current > min) && (current < max)){
  console.log(current);
} 

else if (min > max) {
    console.log("There might be a problem !");
}

else{
    console.log("There might be a problem !");
}